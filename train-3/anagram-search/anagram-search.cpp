#include <bits/stdc++.h>
using namespace std;
#define alphabet 26
string firstLine;
string secondLine;


int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // cin >> firstLine >> secondLine;


    // for(size_t i = (secondLine.size() - 1); i < firstLine.size(); i++){
    //     break;
    // }
    cout << 'a'%'?' << " " << 'z'%'?' << endl;
    cout << (int)'?' << " " << (int)'a' <<  " " << (int)'z' << endl;
    cout << (int)'aba' << " " << (int)'aab' << endl;
    return 0;
}